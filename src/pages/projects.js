const Projects = () => {
    return (<>
        <div className={'container prose'}>
            <p>
                I've not gotten around to finishing this part (yet). Maybe take a look 
                at my GitHub and GitLab accounts (see the index) in the meantime?
            </p>
        </div>
    </>);
};

export default Projects;
